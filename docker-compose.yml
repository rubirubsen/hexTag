version: '3.8'

services:
  hextag-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hextag-web
    restart: unless-stopped
    ports:
      - "8080:80"
    environment:
      - NODE_ENV=production
